# Automate over `az container exec`

This script is an addition to the StackOverflow question I asked and answered here: https://stackoverflow.com/questions/66356445/automate-az-container-exec

It uses a screen multiplexer to automate sending commands over the container terminal.
